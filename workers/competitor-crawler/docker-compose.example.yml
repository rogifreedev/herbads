services:
  competitor-crawler:
    build:
      context: ../..
      dockerfile: workers/competitor-crawler/Dockerfile
    env_file:
      - .env
    restart: unless-stopped
    shm_size: "1gb"
    init: true
